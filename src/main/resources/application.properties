spring.datasource.url=jdbc:postgresql://localhost:5432/student
spring.datasource.username=pedro
# WARNING: Unsafe password on git because why not
# For a new/junior developer, please don't commit this file
# to a public repo, if you're doing code that will go to production
# You need to use ENV variables instead:
# https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.external-config
# https://www.baeldung.com/spring-boot-properties-env-variables
# Check the example for the postgresql database setup on docker. It uses env variables.
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true